<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Sin esto la APK de release no tiene red: Flutter solo declara INTERNET
         en los manifests de debug y profile, que no entran en la compilacion
         final. En debug todo funciona y en release falla cada peticion, que es
         lo que hace que el fallo aparezca justo al instalar la APK. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Permissions for Bluetooth LE -->
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" android:maxSdkVersion="28" />

    <!-- Health Connect Permissions -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION"/>

    <!-- Notificaciones locales (recordatorios de entreno, pesarte y nutricion).
         POST_NOTIFICATIONS es obligatorio desde Android 13: sin el, el sistema
         descarta los avisos en silencio.
         RECEIVE_BOOT_COMPLETED hace que los recordatorios ya programados
         sobrevivan a un reinicio del movil; sin el se pierden todos al apagar.
         No se pide SCHEDULE_EXACT_ALARM a proposito: los recordatorios usan
         alarmas inexactas, que no requieren ese permiso restringido. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
         Servicio en primer plano para el entrenamiento en curso. Sin el, el
         sistema congela la app a los pocos minutos de salir: se para el
         cronometro y se corta el BLE de la pulsera.

         El tipo es connectedDevice y NO health a proposito. Los dos encajarian
         semanticamente, pero en Android 14 el tipo health exige permisos de
         sensores concedidos en runtime, y si faltan el servicio ni arranca.
         connectedDevice solo pide BLUETOOTH_CONNECT, que esta arriba y ya se
         concede para la banda.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.health.READ_EXERCISE" />
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_ACTIVE_CALORIES_BURNED" />
    <uses-permission android:name="android.permission.health.READ_TOTAL_CALORIES_BURNED" />
    <uses-permission android:name="android.permission.health.READ_DISTANCE" />
    <!-- Sueño (Health Connect) — SLEEP_IN_BED / SLEEP_ASLEEP / fases granulares -->
    <uses-permission android:name="android.permission.health.READ_SLEEP" />
    <uses-permission android:name="android.permission.health.READ_SLEEP_SESSION" />
    <!-- CRÍTICO: Sin esto, HC solo devuelve datos de los últimos 30 días desde la primera auth -->
    <uses-permission android:name="android.permission.health.READ_HEALTH_DATA_HISTORY" />

    <application
        android:label="Personal TrAIner"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- REQUERIDO por el paquete health: Health Connect necesita este intent en MainActivity -->
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>

        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Privacy Policy Activity for Health Connect -->
        <activity
            android:name=".HealthConnectPrivacyPolicyActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <!-- Para Android 14+: alias necesario para aparecer en "Aplicaciones de salud" -->
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".HealthConnectPrivacyPolicyActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>

        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="connectedDevice"
            android:exported="false" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Health Connect APK -->
        <package android:name="com.google.android.apps.healthdata" />
        <intent>
            <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
        </intent>
        <!--
          Cámara para image_picker (Físico, Clínica y el chat de Pulso).
          OBLIGATORIO desde Android 11 (API 30): el filtrado de visibilidad de
          paquetes esconde la app de cámara si no se declara aquí, y entonces
          `startActivityForResult(ACTION_IMAGE_CAPTURE)` lanza
          ActivityNotFoundException — image_picker lo traduce a
          `no_available_camera` y la cámara ni se abre. La galería sí funciona
          sin esto, que es lo que hacía parecer que el problema era de permisos.
          El plugin NO lo declara en su propio manifest: es cosa de la app.
        -->
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>
</manifest>
